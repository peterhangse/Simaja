rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads and writes for now (single user app)
    // In production, you'd add authentication checks
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
